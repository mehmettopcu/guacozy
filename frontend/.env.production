# Production build settings (read by Create React App during `npm run build`).
# Disable source maps so:
# - source-map-loader does not choke on deps that ship broken sourcemap
#   references (e.g. flexlayout-react), and
# - the app's source maps are not shipped in the built bundle.
GENERATE_SOURCEMAP=false
