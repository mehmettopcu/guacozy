# Several UI libraries (semantic-ui-react 0.88, flexlayout-react 0.3,
# react-contexify 4, rc-tree 2) still declare a react ^16 peer dependency but
# work under React 18 at runtime (verified by the E2E suite). Allow the install
# instead of failing on those stale peer ranges.
legacy-peer-deps=true
