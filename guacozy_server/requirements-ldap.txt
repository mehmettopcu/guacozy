-r requirements-base.txt
# django-auth-ldap 4.6.0 supports Django 4.2.
# Note: LDAP is not exercised by CI (no directory server), so these pins are
# upgraded for compatibility but verified manually.
django-auth-ldap==4.6.0
python-ldap==3.4.4; sys_platform == 'linux'
